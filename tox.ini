[tox]
envlist =
    py{312,313,314}-test{,-alldeps}{,-oldestdeps,-devdeps,-predeps}{,-cov}
    py315-test-devdeps
    linkcheck
    codestyle
requires =
    tox >= 4
    tox-uv >= 1.35

[testenv]

# Pass through the following environment variables which may be needed for the CI
passenv = HOME,WINDIR,CI

setenv =
    # Suppress display of matplotlib plots generated during the tests
    MPLBACKEND = agg
    devdeps: UV_EXTRA_INDEX_URL = https://pypi.anaconda.org/astropy/simple https://pypi.anaconda.org/liberfa/simple https://pypi.anaconda.org/scientific-python-nightly-wheels/simple
    # Consider every index for every package (pip's behaviour), since the
    # nightly indexes only carry some packages or some of their builds.
    devdeps: UV_INDEX_STRATEGY = unsafe-best-match
    cov: COVERAGE_FILE = {toxinidir}/.coverage
    oldestdeps: UV_CONSTRAINT = {toxinidir}/oldestdeps-constraints.txt

# Run the tests in a temporary directory to make sure that we don't import
# this package from the source tree
changedir = .tmp/{envname}

# tox environments are constructed with so-called 'factors' (or terms)
# separated by hyphens, e.g. test-devdeps-cov. Lines below starting with factor:
# will only take effect if that factor is included in the environment name. To
# see a list of example environments that can be run, along with a description,
# run:
#
#     tox -l -v
#
description =
    run tests
    alldeps: with optional dependencies
    oldestdeps: with the oldest supported versions of all dependencies
    devdeps: with the latest developer version of key dependencies
    predeps: with pre-releases of key dependencies
    cov: with test coverage

# The following provides some specific pinnings for key packages
deps =
    devdeps: numpy>=0.0.dev0
    devdeps: scipy>=0.0.dev0
    devdeps: pyerfa>=0.0.dev0
    devdeps: astropy>=0.0.dev0
    # Falls back to the latest release on Pythons the nightly index doesn't
    # build yet (e.g. matplotlib on 3.15 pre-releases)
    devdeps: matplotlib>=0.0.dev0
    devdeps: specutils @ git+https://github.com/astropy/specutils.git
    devdeps: photutils>=0.0.dev0
    devdeps: synphot @ git+https://github.com/spacetelescope/synphot_refactor.git

# The following indicates which optional dependencies will be installed
extras =
    test
    alldeps, oldestdeps: all

commands =
    {list_dependencies_command}
    !cov: pytest --pyargs specreduce {toxinidir}/docs {posargs}
    cov: pytest --pyargs specreduce {toxinidir}/docs --cov specreduce --cov-config={toxinidir}/pyproject.toml {posargs}

pip_pre =
    devdeps, predeps: true

# oldestdeps resolves every requirement, including transitive ones, to the
# lowest version allowed by the declared lower bounds, so the minimum versions
# in pyproject.toml are what get tested rather than hand-maintained pins.
# Lower bounds for transitive dependencies live in oldestdeps-constraints.txt
# (applied via UV_CONSTRAINT above).
uv_resolution =
    oldestdeps: lowest

[testenv:linkcheck]
changedir = docs
description = check the links in the HTML docs
extras = docs
commands =
    {list_dependencies_command}
    sphinx-build -W -b linkcheck . _build/html

[testenv:codestyle]
skip_install = true
changedir = .
description = check code style, e.g., with flake8
deps = flake8
commands = flake8 specreduce --count --extend-ignore E203
